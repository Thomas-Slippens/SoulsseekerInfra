apiVersion: v1
kind: Service
metadata:
  name: feedback
spec:
  type: ClusterIP

  selector:
    app: feedback

  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP